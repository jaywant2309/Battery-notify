# Postgres connection
PG_CONNECTION_STRING=postgres://test:test@localhost:5432/battery

# DynamoDB local endpoint (for local dev) or omit for AWS
DYNAMO_ENDPOINT=http://localhost:8000
DYNAMO_TABLE_LOCKS=locks
AWS_REGION=us-east-1

# Firebase
GOOGLE_APPLICATION_CREDENTIALS=/path/to/firebase-service-account.json

# Click tracking endpoint base URL (public or ngrok during dev)
TRACKING_BASE_URL=http://localhost:3000/track_click

# Optional
CAMPAIGN_NAME=weekly_battery_check

# Server port for tracker
PORT=3000
